import 'package:flutter/material.dart';

class Bkash extends StatelessWidget {
  const Bkash({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bkash Payment'),
      ),

      body: Center(
        child:Text('bkash number')
      ),

    );

  }
}
